import React from 'react';import Button from './button';class Playground extends React.Component {  state = { selected: false };  updateSelected = (value, option) => {    console.log(value, option);    return this.setState({      selected: value,      option: [value ? option : null]    })  }  render() {    return (        <div>          <h3>Ques - { this.props.number }</h3>          <p>{ this.props.question }</p>          <ol>            {              this.props.options.map((option) => {                return (                    <li key={ option }>                      <input                          type="radio"                          id={ option }                          name={ this.props.number }                          value={this.state.selected}                          onChange={(e) => this.updateSelected(e.target.checked, option)}                      />                      <label htmlFor={ option }>{ option }</label>                    </li>                );              })            }          </ol>          <Button              onclick={ this.props.onclick }              args={ this.state.selected ? this.state.option : null }>            Next</Button>        </div>    );  }}export default Playground;